name: guido-cuda
channels:
  - pytorch
  - conda-forge
  - nvidia
dependencies:
  - python=3.8.10
  - cudatoolkit=11.6
  - tensorflow=2.10.*
  - pip
  - poetry
  - pip:
    - setuptools
    - wheel
    - hydra-joblib-launcher == 1.2.0

